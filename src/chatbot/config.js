import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
  initialMessages: [createChatBotMessage(`What are your questions regarding your provided resources?`)],
};

export default config;